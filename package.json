{
  "name": "the-recipe-box",
  "version": "1.0.0",
  "description": "A recipe management application powered by Firebase Auth + Firestore.",
  "main": "script.js",
  "type": "module",
  "scripts": {
    "build:config": "node scripts/generateFirebaseConfig.js",
    "serve": "npm run build:config && npx serve .",
    "start": "npm run serve"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "serve": "^14.2.1"
  }
}
